import type { Metadata } from "next";
import { PageHero } from "@/components/PageHero";
import { ContactForm } from "@/components/ContactForm";
import { siteConfig, whatsappUrl } from "@/data/siteConfig";
export const metadata:Metadata={title:"Contact",description:"Begin an interior design, architecture or furniture conversation with Atelier Arka.",alternates:{canonical:"/contact"}};
export default function ContactPage(){return <><PageHero eyebrow="New enquiries / Contact" title="Let’s create" italic="something exceptional." intro="Tell us what you are imagining, where you are in the journey, and what matters most. We usually respond within two working days."/><section className="pb-28 md:pb-44"><div className="container-site grid gap-16 lg:grid-cols-12"><div className="lg:col-span-8"><ContactForm/></div><aside className="lg:col-span-4 lg:border-l lg:border-[var(--line)] lg:pl-10"><p className="eyebrow text-accent">Speak directly</p><a href={whatsappUrl()} target="_blank" className="serif mt-5 block text-3xl">WhatsApp us →</a><a href={`tel:${siteConfig.phone.replaceAll(" ","")}`} className="mt-5 block text-sm">{siteConfig.phone}</a><a href={`mailto:${siteConfig.email}`} className="mt-2 block text-sm">{siteConfig.email}</a><div className="mt-14"><p className="eyebrow text-accent">Visit by appointment</p><address className="mt-5 max-w-xs text-sm not-italic leading-7 text-muted">{siteConfig.address}</address></div><div className="mt-14"><p className="eyebrow text-accent">Follow the process</p><div className="mt-5 flex gap-5 text-sm"><a href={siteConfig.socials.instagram}>Instagram</a><a href={siteConfig.socials.pinterest}>Pinterest</a></div></div></aside></div></section></>}
